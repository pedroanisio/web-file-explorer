# Debug mode (0=off, 1=on)
FLASK_DEBUG=0

# Host directory to mount in the container
# This directory will be accessible through the file explorer
HOST_DIR=/path/to/your/files

# Port to expose the application on
# Default is 5000, change if needed
PORT=5000